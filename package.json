{
  "name": "cloudflare-worker-update-community-apps-themes-downloads",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev --test-scheduled",
    "start": "wrangler dev --test-scheduled"
  },
  "devDependencies": {
    "wrangler": "^3.60.3"
  },
  "dependencies": {
    "base-64": "^1.0.0",
    "json-bigint": "^1.0.0",
    "realm-web": "^2.0.1",
    "toucan-js": "^4.0.0"
  }
}
